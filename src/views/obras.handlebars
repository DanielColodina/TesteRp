 
<link rel="stylesheet" href="/css/tables-users.css?v=2.1">

<div class="app-layout">
  <main class="main-content">
    <div class="top-bar">
      <h1>🏗️ Gerenciamento de Obras</h1>
      <div class="flex gap-10">
        <a href="/obras/nova" class="btn btn-primary">➕ Nova Obra</a>
        <a href="/dashboard" class="btn btn-secondary">← Voltar</a>
      </div>
    </div>

    <!-- BUSCA DE OBRAS -->
    <div class="search-container">
      <form action="/obras" method="GET" class="search-form">
        <input type="text" name="search" id="searchInput" placeholder="Buscar obras pelo nome..." value="{{#if search}}{{search}}{{/if}}" autocomplete="off">
        <datalist id="sugestoesObras"></datalist>
        <button type="submit" class="btn-search">Buscar</button>
        {{#if search}}
          <a href="/obras" class="btn-clear">Limpar</a>
        {{/if}}
      </form>
    </div>


    <div class="app-container">
      {{#if obras}}
        <div class="card">
          <h2><i class="fas fa-building"></i> Lista de Obras</h2>
          <div class="table-container">
            <table class="table">
              <thead>
                <tr>
                  <th>Registro</th>
                  <th>Nome da Obra</th>
                  <th>Cliente</th>
                  <th>Endereço</th>
                  <th>Descrição</th>
                  <th>Criada em</th>
                  <th>Ações</th>
                </tr>
              </thead>
              <tbody>
                {{#each obras}}
                  <tr>
                    <td><a href="/rotascompletas" class="btn-action btn-view" title="Registro Obras">📋</a></td>
                    <td class="cell-nome">{{nome_obra}}</td>
                    <td>{{usuario_nome}}</td>
                    <td>{{endereco_obra}}</td>
                    <td class="cell-descricao">{{descricao}}</td>
                    <td class="cell-data">{{created_at}}</td>
                    <td class="cell-actions">
                      <a href="/obras/{{id}}/editar" class="btn-action btn-edit" title="Editar">✏️</a>
                      <form action="/obras/{{id}}/excluir" method="POST" style="display: inline;" onsubmit="return confirm('Tem certeza que deseja excluir esta obra?')">
                        <button type="submit" class="btn-action btn-delete" title="Excluir">🗑️</button>
                      </form>
                    </td>
                  </tr>
                {{/each}}
              </tbody>
            </table>
          </div>
        </div>
      {{else}}
        <div class="empty-state glass-widget">
          <div class="empty-icon">🏗️</div>
          <h3>Nenhuma obra cadastrada ainda</h3>
          <p>Clique em "Nova Obra" para começar a gerenciar suas obras.</p>
          <a href="/obras/nova" class="btn-nova-obra btn-large">➕ Criar Primeira Obra</a>
        </div>
      {{/if}}
    </div>
  </main>
</div>

<script>
// Autocomplete para busca de obras
document.addEventListener('DOMContentLoaded', function() {
    const searchInput = document.getElementById('searchInput');
    const sugestoesDatalist = document.getElementById('sugestoesObras');
    let timeout = null;

    if (searchInput && sugestoesDatalist) {
        searchInput.addEventListener('input', function() {
            const termo = this.value.trim();
            
            clearTimeout(timeout);
            
            if (termo.length < 1) {
                sugestoesDatalist.innerHTML = '';
                return;
            }
            
            timeout = setTimeout(function() {
                buscarSugestoes(termo);
            }, 300);
        });
    }

    function buscarSugestoes(termo) {
        fetch(`/obras/search?q=${encodeURIComponent(termo)}`)
            .then(response => response.json())
            .then(data => {
                sugestoesDatalist.innerHTML = '';
                data.forEach(obra => {
                    const option = document.createElement('option');
                    option.value = obra.nome_obra;
                    sugestoesDatalist.appendChild(option);
                });
            })
            .catch(err => {
                console.error('Erro ao buscar sugestões:', err);
            });
    }
});
</script>

