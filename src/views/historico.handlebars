<div class="historico-layout">
  <aside class="sidebar sidebar-closed">
    <div class="sidebar-header">
      <div class="logo">RP 🏗️</div>
    </div>
    <nav class="sidebar-nav">
      <a href="/dashboard" class="nav-item">
        <span>📊</span> Dashboard
      </a>
      <a href="/dashboard/usuarios" class="nav-item">
        <span>👥</span> Usuários
      </a>
      <a href="/dashboard/obras" class="nav-item">
        <span>🏗️</span> Obras
      </a>
      <a href="/dashboard/progresso" class="nav-item">
        <span>📈</span> Progresso
      </a>
      <a href="/dashboard/controle-geral" class="nav-item">
        <span>🎛️</span> Controle
      </a>
      <a href="/dashboard/historico" class="nav-item active">
        <span>📜</span> Histórico
      </a>
      <a href="/dashboard/auditoria" class="nav-item">
        <span>🔒</span> Auditoria
      </a>
    </nav>
    <div class="sidebar-footer">
      <a href="/logout" class="btn-logout">Sair</a>
    </div>
  </aside>

  <main class="main-content sidebar-closed">
    <div class="top-bar">
      <button class="sidebar-toggle">
        <i class="fas fa-bars"></i>
      </button>
      <h1>📜 Histórico Completo de Mudanças</h1>
      <a href="/dashboard" class="btn-primary-top">← Voltar</a>
    </div>

    <div class="historico-container">
      {{#if historico}}
        <div class="historico-header glass-widget">
          <p style="text-align: center; color: #FFC300; margin: 0; font-weight: 700;">
            Total de mudanças: <strong style="font-size: 1.4em;">{{historico.length}}</strong>
          </p>
        </div>

        <div class="historico-list">
          {{#each historico}}
            <div class="historico-item glass-widget">
              <div class="historico-grid">
                <div class="historico-field">
                  <label>Usuário</label>
                  <p>{{usuario_nome}}</p>
                </div>
                <div class="historico-field">
                  <label>Obra</label>
                  <p>{{obra}}</p>
                </div>
                <div class="historico-field">
                  <label>Tipo</label>
                  <p class="tipo-badge">{{tipo}}</p>
                </div>
                <div class="historico-field">
                  <label>Admin Responsável</label>
                  <p>{{admin_nome}}</p>
                </div>
              </div>

              <div class="historico-description">
                <label>📝 Descrição</label>
                <p>{{descricao}}</p>
              </div>

              <div class="historico-timestamp">
                <small>📅 {{created_at}}</small>
              </div>
            </div>
          {{/each}}
        </div>
      {{else}}
        <div class="glass-widget" style="text-align: center; padding: 60px 20px;">
          <p style="color: #B0B0B0; font-size: 1.1em; margin: 0;">Nenhum histórico de mudanças encontrado.</p>
        </div>
      {{/if}}
    </div>
  </main>
</div>

<style>
/* ===== PALETA DE CORES ===== */
:root {
  --bg-dark: #0A0A0A;
  --bg-darker: #1a1a1a;
  --glass-bg: rgba(30, 30, 30, 0.4);
  --glass-hover: rgba(30, 30, 30, 0.55);
  --yellow-primary: #FFC300;
  --yellow-hover: #FFD700;
  --text-primary: #FFFFFF;
  --text-secondary: #B0B0B0;
  --glass-border: rgba(255, 255, 255, 0.08);
}

/* ===== GLASS WIDGET BASE ===== */
.glass-widget {
  background: var(--glass-bg);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid var(--glass-border);
  box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
  border-radius: 16px;
  padding: 20px;
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.glass-widget:hover {
  background: var(--glass-hover);
  border: 1px solid rgba(255, 195, 0, 0.15);
  box-shadow: 0 12px 40px 0 rgba(255, 195, 0, 0.15);
  transform: translateY(-4px);
}

/* ===== LAYOUT PRINCIPAL ===== */
.historico-layout {
  display: flex;
  min-height: 100vh;
  background: linear-gradient(135deg, var(--bg-dark) 0%, var(--bg-darker) 100%);
  font-family: Inter, Roboto, Poppins, sans-serif;
  color: var(--text-primary);
}

/* ===== SIDEBAR ===== */
.sidebar {
  width: 280px;
  background: rgba(15, 15, 15, 0.8);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border-right: 2px solid var(--yellow-primary);
  position: fixed;
  height: 100vh;
  left: 0;
  top: 0;
  z-index: 999;
  display: flex;
  flex-direction: column;
  overflow-y: auto;
}

.sidebar-header {
  padding: 30px 20px;
  text-align: center;
  border-bottom: 1px solid rgba(255, 195, 0, 0.2);
}

.logo {
  font-size: 1.8rem;
  font-weight: 800;
  background: linear-gradient(135deg, var(--yellow-primary) 0%, var(--yellow-hover) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.sidebar-nav {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding: 20px 10px;
}

.nav-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px 16px;
  color: var(--text-secondary);
  text-decoration: none;
  border-radius: 8px;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  font-weight: 500;
}

.nav-item:hover,
.nav-item.active {
  background: rgba(255, 195, 0, 0.1);
  color: var(--yellow-primary);
  border-left: 3px solid var(--yellow-primary);
  padding-left: 13px;
}

.nav-item span {
  font-size: 1.2rem;
}

.sidebar-footer {
  padding: 20px 10px;
  border-top: 1px solid rgba(255, 195, 0, 0.2);
}

.btn-logout {
  display: block;
  padding: 12px 16px;
  background: rgba(255, 67, 67, 0.1);
  color: #FF6B6B;
  border-radius: 8px;
  text-decoration: none;
  text-align: center;
  font-weight: 600;
  transition: all 0.3s;
}

.btn-logout:hover {
  background: rgba(255, 67, 67, 0.2);
  transform: translateY(-2px);
}

/* ===== MAIN CONTENT ===== */
.main-content {
  flex: 1;
  margin-left: 280px;
  overflow-y: auto;
  background: linear-gradient(135deg, var(--bg-dark) 0%, var(--bg-darker) 100%);
}

.top-bar {
  position: sticky;
  top: 0;
  z-index: 100;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 30px 40px;
  background: rgba(10, 10, 10, 0.6);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border-bottom: 1px solid rgba(255, 195, 0, 0.1);
}

.top-bar h1 {
  margin: 0;
  font-size: 2rem;
  background: linear-gradient(135deg, var(--yellow-primary) 0%, var(--yellow-hover) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  font-weight: 800;
}

.btn-primary-top {
  padding: 10px 20px;
  background: linear-gradient(135deg, var(--yellow-primary) 0%, var(--yellow-hover) 100%);
  color: var(--bg-dark);
  text-decoration: none;
  border-radius: 8px;
  font-weight: 700;
  border: none;
  cursor: pointer;
  transition: all 0.3s;
  box-shadow: 0 4px 15px rgba(255, 195, 0, 0.25);
}

.btn-primary-top:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(255, 195, 0, 0.35);
}

/* ===== HISTORICO CONTAINER ===== */
.historico-container {
  padding: 40px;
  max-width: 1400px;
  margin: 0 auto;
}

.historico-header {
  margin-bottom: 30px;
  text-align: center;
}

.historico-list {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.historico-item {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.historico-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 15px;
}

.historico-field {
  display: flex;
  flex-direction: column;
}

.historico-field label {
  font-size: 0.9rem;
  color: var(--yellow-primary);
  font-weight: 600;
  margin-bottom: 5px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.historico-field p {
  margin: 0;
  color: var(--text-primary);
}

.tipo-badge {
  background: rgba(255, 195, 0, 0.2);
  padding: 4px 12px;
  border-radius: 20px;
  color: var(--yellow-primary);
  width: fit-content;
}

.historico-description {
  padding: 20px;
  background: rgba(255, 195, 0, 0.05);
  border-left: 3px solid var(--yellow-primary);
  border-radius: 8px;
}

.historico-description label {
  font-size: 0.9rem;
  color: var(--yellow-primary);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  display: block;
  margin-bottom: 10px;
}

.historico-description p {
  margin: 0;
  color: var(--text-primary);
  line-height: 1.6;
}

.historico-timestamp {
  padding-top: 15px;
  border-top: 1px solid rgba(255, 195, 0, 0.1);
}

.historico-timestamp small {
  color: var(--text-secondary);
  font-size: 0.85rem;
}

/* ===== CUSTOM SCROLLBAR ===== */
.main-content::-webkit-scrollbar {
  width: 8px;
}

.main-content::-webkit-scrollbar-track {
  background: rgba(0, 0, 0, 0.2);
  border-radius: 10px;
}

.main-content::-webkit-scrollbar-thumb {
  background: linear-gradient(180deg, var(--yellow-primary) 0%, var(--yellow-hover) 100%);
  border-radius: 10px;
}

.main-content::-webkit-scrollbar-thumb:hover {
  background: var(--yellow-primary);
}

/* ===== RESPONSIVE DESIGN ===== */
@media (max-width: 1024px) {
  .top-bar {
    padding: 20px 30px;
  }

  .historico-container {
    padding: 30px;
  }
}

@media (max-width: 768px) {
  .sidebar {
    width: 70px;
  }

  .main-content {
    margin-left: 70px;
  }

  .sidebar-header {
    padding: 20px 10px;
  }

  .logo {
    font-size: 1.2rem;
  }

  .nav-item span {
    font-size: 1rem;
  }

  .nav-item {
    justify-content: center;
    padding: 12px 8px;
  }

  .top-bar {
    flex-direction: column;
    gap: 15px;
    padding: 20px;
  }

  .top-bar h1 {
    font-size: 1.5rem;
  }

  .historico-grid {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 480px) {
  .sidebar {
    width: 60px;
  }

  .main-content {
    margin-left: 60px;
  }

  .top-bar {
    padding: 15px;
    gap: 10px;
  }

  .top-bar h1 {
    font-size: 1.3rem;
  }

  .historico-container {
    padding: 15px;
  }

  .historico-item {
    padding: 15px;
  }
}
</style>


