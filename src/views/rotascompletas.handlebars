<link rel="stylesheet" href="/css/dashboard-progresso.css?v=2.1">

<div class="app-layout">
  <main class="main-content">
    <div class="top-bar">
      <h1>📋 Registro Obras - Todas as Obras</h1>
      <a href="/obras" class="btn btn-secondary">🔙 Voltar às Obras</a>
    </div>

    <div class="app-container">
      {{#if obras}}
        <!-- LISTA DE OBRAS EXPANDÍVEIS -->
        <div class="accordion" id="obrasAccordion">
          {{#each obras}}
            <div class="accordion-item" data-obra-id="{{id}}">
              <button class="accordion-header" data-toggle="accordion" aria-expanded="false">
                <span class="obra-title">
                  <strong>{{nome_obra}}</strong>
                  <span class="obra-cliente">{{cliente_nome}}</span>
                </span>
                <span class="accordion-icon">▼</span>
              </button>
              
              <div class="accordion-content" id="content-{{id}}" style="display: none;">
                <div class="loading-spinner">
                  <i class="fas fa-spinner fa-spin"></i> Carregando dados...
                </div>
              </div>
            </div>
          {{/each}}
        </div>
      {{else}}
        <div class="empty-state glass-widget">
          <div class="empty-icon">🏗️</div>
          <h3>Nenhuma obra cadastrada</h3>
          <p>Não há obras para exibir.</p>
        </div>
      {{/if}}
    </div>
  </main>
</div>

<style>
/* Accordion Styles */
.accordion {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.accordion-item {
  background: linear-gradient(135deg, rgba(31, 31, 46, 0.8), rgba(26, 26, 38, 0.6));
  border-radius: 12px;
  border: 1px solid rgba(255, 255, 255, 0.08);
  overflow: hidden;
}

.accordion-header {
  width: 100%;
  padding: 20px 24px;
  background: none;
  border: none;
  display: flex;
  justify-content: space-between;
  align-items: center;
  cursor: pointer;
  color: #FFFFFF;
  font-size: 1rem;
  transition: background 0.3s ease;
}

.accordion-header:hover {
  background: rgba(255, 195, 0, 0.05);
}

.accordion-header.active {
  background: rgba(255, 195, 0, 0.1);
}

.obra-title {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 4px;
}

.obra-title strong {
  font-size: 1.1rem;
  color: #FFC300;
}

.obra-cliente {
  font-size: 0.9rem;
  color: #C0C0C0;
}

.accordion-icon {
  font-size: 0.8rem;
  color: #FFC300;
  transition: transform 0.3s ease;
}

.accordion-header.active .accordion-icon {
  transform: rotate(180deg);
}

.accordion-content {
  max-height: none;
  overflow: visible;
}

.loading-spinner {
  padding: 40px;
  text-align: center;
  color: #FFC300;
  font-size: 1.1rem;
}

.loading-spinner i {
  margin-right: 10px;
}

/* Info Section Styles */
.info-section {
  padding: 20px 24px;
  border-bottom: 1px solid rgba(255, 255, 255, 0.05);
}

.info-section:last-child {
  border-bottom: none;
}

.info-section h3 {
  color: #FFC300;
  font-size: 1rem;
  margin-bottom: 16px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16px;
}

.section-header h3 {
  margin-bottom: 0;
}

.btn-add {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 32px;
  height: 32px;
  background: linear-gradient(135deg, #10b981, #34d399);
  color: white;
  border-radius: 50%;
  font-size: 1.2rem;
  font-weight: bold;
  text-decoration: none;
  transition: all 0.3s ease;
  box-shadow: 0 2px 8px rgba(16, 185, 129, 0.3);
}

.btn-add:hover {
  transform: scale(1.1);
  box-shadow: 0 4px 12px rgba(16, 185, 129, 0.5);
  background: linear-gradient(135deg, #059669, #10b981);
}

.info-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 16px;
}

.info-item {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.info-item label {
  color: #C0C0C0;
  font-size: 0.85rem;
}

.info-item span {
  color: #FFFFFF;
  font-size: 0.95rem;
}

/* Progress Bar */
.progress-overview {
  margin-bottom: 20px;
}

.progress-bar-container.large {
  height: 32px;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 16px;
  overflow: hidden;
}

.progress-bar.large {
  height: 100%;
  background: linear-gradient(90deg, #FFC300, #FFD700);
  border-radius: 16px;
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 70px;
  overflow: visible;
  position: relative;
  z-index: 1;
}

.progress-text {
  color: #1a1a1a;
  font-weight: 700;
  font-size: 0.9rem;
  white-space: nowrap;
  padding: 0 8px;
}

/* Checklist Grid */
.checklist-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
  gap: 12px;
}

.checklist-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  padding: 16px;
  background: rgba(255, 255, 255, 0.03);
  border-radius: 8px;
  text-align: center;
}

.checklist-item.Feito {
  background: rgba(16, 185, 129, 0.1);
  border: 1px solid rgba(16, 185, 129, 0.3);
}

.checklist-item.Andamento {
  background: rgba(245, 158, 11, 0.1);
  border: 1px solid rgba(245, 158, 11, 0.3);
}

.checklist-item.Nao\ Tem {
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid rgba(255, 255, 255, 0.08);
}

.checklist-icon {
  font-size: 1.5rem;
}

.checklist-status {
  font-size: 0.8rem;
  color: #C0C0C0;
}

/* Materiais List */
.materiais-list, .funcionarios-list {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.material-item, .funcionario-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 16px;
  background: rgba(255, 255, 255, 0.03);
  border-radius: 8px;
}

.material-info, .funcionario-info {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.material-quantidade {
  font-size: 0.85rem;
  color: #10b981;
}

.material-fase {
  font-size: 0.8rem;
  color: #FFC300;
  padding: 4px 8px;
  background: rgba(255, 195, 0, 0.1);
  border-radius: 4px;
}

/* Financeiro Resumo */
.financeiro-resumo {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
  margin-bottom: 20px;
}

.resumo-item {
  padding: 16px;
  border-radius: 8px;
  text-align: center;
}

.resumo-item.ganho {
  background: rgba(16, 185, 129, 0.1);
  border: 1px solid rgba(16, 185, 129, 0.3);
}

.resumo-item.despesa {
  background: rgba(239, 68, 68, 0.1);
  border: 1px solid rgba(239, 68, 68, 0.3);
}

.resumo-item.saldo {
  background: rgba(59, 130, 246, 0.1);
  border: 1px solid rgba(59, 130, 246, 0.3);
}

.resumo-label {
  display: block;
  font-size: 0.85rem;
  color: #C0C0C0;
  margin-bottom: 8px;
}

.resumo-valor {
  font-size: 1.2rem;
  font-weight: 700;
  color: #FFFFFF;
}

.resumo-item.ganho .resumo-valor { color: #10b981; }
.resumo-item.despesa .resumo-valor { color: #ef4444; }
.resumo-item.saldo .resumo-valor { color: #3b82f6; }

/* Financeiro List */
.financeiro-list {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.financeiro-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 16px;
  background: rgba(255, 255, 255, 0.03);
  border-radius: 8px;
}

.financeiro-item.entrada, .financeiro-item.receita {
  border-left: 3px solid #10b981;
}

.financeiro-item.saida, .financeiro-item.despesa {
  border-left: 3px solid #ef4444;
}

.financeiro-info {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.financeiro-info strong {
  color: #FFFFFF;
}

.financeiro-tipo {
  font-size: 0.75rem;
  padding: 2px 8px;
  border-radius: 4px;
  background: rgba(255, 255, 255, 0.1);
}

.financeiro-valores {
  text-align: right;
}

.financeiro-valor {
  display: block;
  font-weight: 700;
}

.financeiro-valor.entrada, .financeiro-valor.receita {
  color: #10b981;
}

.financeiro-valor.saida, .financeiro-valor.despesa {
  color: #ef4444;
}

.financeiro-data {
  font-size: 0.8rem;
  color: #C0C0C0;
}

/* Histórico */
.historico-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.historico-item {
  padding: 12px 16px;
  background: rgba(255, 255, 255, 0.03);
  border-radius: 8px;
}

.historico-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 8px;
}

.historico-header strong {
  color: #FFC300;
}

.historico-data {
  font-size: 0.8rem;
  color: #C0C0C0;
}

.historico-item p {
  color: #FFFFFF;
  margin-bottom: 8px;
}

.historico-item small {
  color: #666;
}

/* Empty State */
.no-data {
  color: #C0C0C0;
  text-align: center;
  padding: 20px;
  font-style: italic;
}

@media (max-width: 768px) {
  .financeiro-resumo {
    grid-template-columns: 1fr;
  }
}
</style>

<script src="/js/rotascompletas.js"></script>
